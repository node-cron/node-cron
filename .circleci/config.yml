version: 2.1

orbs:
  node: circleci/node@4.7

jobs:
  test:
    executor: node/default
    steps:
      - checkout
      - run: npm install
      - run: COVERALLS_GIT_BRANCH=$CIRCLE_BRANCH npm run check

workflows:
  build:
    jobs:
      - test
